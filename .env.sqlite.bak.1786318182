DATABASE_URL=file:/home/z/my-project/db/custom.db

# === URL pública de la aplicación ===
# Usada por src/lib/url.ts -> getBaseUrl() para generar links absolutos
# enviados al cliente (firma, T&C, pagos, recibos).
# En Vercel: configurar también como variable de entorno del proyecto.
NEXT_PUBLIC_BASE_URL=https://jsadr.com.co
NEXT_PUBLIC_APP_URL=https://jsadr.com.co
